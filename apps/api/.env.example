# Copy to .env and adjust.
DATABASE_URL="file:./dev.db"
PORT=3001
# AI provider: "mock" (default, no deps) | "ollama"
AI_PROVIDER=mock
# Ollama settings (used only when AI_PROVIDER=ollama)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=gemma3:4b
